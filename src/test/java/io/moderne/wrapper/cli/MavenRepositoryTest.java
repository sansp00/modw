package io.moderne.wrapper.cli;

import static org.assertj.core.api.Assertions.assertThat;

import java.nio.file.Paths;
import java.util.Arrays;
import java.util.Set;

import org.eclipse.aether.artifact.Artifact;
import org.eclipse.aether.installation.InstallationException;
import org.eclipse.aether.resolution.ArtifactResolutionException;
import org.eclipse.aether.resolution.VersionRangeResolutionException;
import org.junit.jupiter.api.Test;

class MavenRepositoryTest {

//	@Test
//	void resolveArtifact() throws ArtifactResolutionException {
//
//		Artifact artifact = new MavenRepository(Paths.get("repo")).resolveArtifact(
//				MavenRepository.getArtifact("org.apache.commons", "commons-lang3", "RELEASE"),
//				Arrays.asList(MavenRepository.getCentralMavenRepository()));
//
//		assertThat(artifact).isNotNull();
//	}
//
//	@Test
//	void resolveVersion() throws ArtifactResolutionException, VersionRangeResolutionException, InstallationException {
//		Set<String> versions = new MavenRepository(Paths.get("repo")).resolveVersion(
//				MavenRepository.getArtifact("org.apache.commons", "commons-lang3", "RELEASE"),
//				Arrays.asList(MavenRepository.getCentralMavenRepository()));
//
//		assertThat(versions).isNotNull();
//	}

}
